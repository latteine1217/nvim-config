--[[
Module: config.autocmds
Purpose: 個人 autocmd delta。
Note: yank 高亮、游標復位、自動建目錄、VimResized 重排、close-with-q、
      TermOpen 等先前手刻的 autocmd 皆已內建於 LazyVim，故此處留空。
      日後若需新增專案專屬 autocmd，寫在這裡即可（LazyVim 會自動載入）。
--]]
